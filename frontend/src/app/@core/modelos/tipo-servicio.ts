export class TipoServicio {
    
     cod_servicio: number;
     nombre: string;
     estado: number;
     edad_servicio: number;

     /*
     constructor(cod_tipo_sangre: number, nombre: string, grupo: string, estado: number){
       this.cod_tipo_sangre = cod_tipo_sangre;
       this.nombre = nombre;
       this.grupo = grupo;
       this.estado = estado;
     } 
     */   
}