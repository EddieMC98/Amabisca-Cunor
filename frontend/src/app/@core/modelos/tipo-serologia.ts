export class TipoSerologia {
    
     cod_serologia: number;
     nombre: string;
     precio: number;
     estado: number;
     prueba_rapida: number;

     /*
     constructor(cod_tipo_sangre: number, nombre: string, grupo: string, estado: number){
       this.cod_tipo_sangre = cod_tipo_sangre;
       this.nombre = nombre;
       this.grupo = grupo;
       this.estado = estado;
     } 
     */   
}