export class TransNoUtilizada {
    
     cod_trans_no_util: number;
     fec_creacion: Date;
     cantidad: number;
     cod_tipo_sangre: number;

     /*
     constructor(cod_tipo_sangre: number, nombre: string, grupo: string, estado: number){
       this.cod_tipo_sangre = cod_tipo_sangre;
       this.nombre = nombre;
       this.grupo = grupo;
       this.estado = estado;
     } 
     */   
}